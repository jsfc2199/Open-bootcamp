"""
Lo que hago en python no puedo ejecutarlo directamente en mi máquina, ya que necesito otro programa, por lo que python es un
lenguaje interpretado, donde el interprete de python es el propio python

Python viene con el comando pip donde instalamos librerias de python
En caso de no estar instalado en linux usamos sudo easy_intall pip
En windows viene por defecto

Si quiero instalar scify escribo pip install scipy y para desintalar escribo pip uninstall scipy

Si necesito dependencias igual usamos el pip install

En caso de dudas usamos el comando help() y me salen las ayudas, ejemplo helo(numpy)

Para recurrir a paquetes vamos a la pagina pypi.org, y allí encontraremos las librerias que podemos usar con pip

Para ver lo que tengo instalado hacemos pip list
Si quiero dados de un paquete instalado pip show 'nombrePaquete' es decir pip show numpy

Pip no puede tener multiples versiones de un mismo paquete, pero podemos usar virtualenvs, lo que crea un entorno virtual en X
proyecto para poder usar diferentes versiones de un paquete, es decir, en un proyecto puedo tener Flask con la version 1 y
otro con la versión 2

Otra forma de tener multiples versiones es pyenv, pero no funciona en windows, pero ambos sirven para lo mismo, simular entornos

"""
